#include <stdio.h>
#include "ft_stock_str.h"

 struct s_stock_str *ft_strs_to_tab(int ac, char **av)
{
	struct s_stock_str str;
	struct s_stock_str *tab;


	while (ac > 0)
	{

	}
	tab = malloc(sizeof(str) * ac);
}

int	main(void)
{
	t_stock_str str;
	printf("%lu", sizeof(str));
}